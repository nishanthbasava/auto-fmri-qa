# Runtime deps for the Docker image (render + llm + web extras), pinned loosely.
# Kept separate from pyproject.toml so Docker can install dependencies in a
# cached layer before the source is copied. For development use:
#     pip install -e .[all,dev]
pyyaml>=6.0
pydantic>=2.0
Pillow>=10.0
playwright>=1.40
anthropic>=0.40
chromadb>=1.0
python-dotenv>=1.0
fastapi>=0.110
uvicorn>=0.29
