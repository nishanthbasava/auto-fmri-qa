pyyaml>=6.0
playwright>=1.40        # SVG rendering via headless chromium (run: playwright install chromium)
Pillow>=10.0
anthropic>=0.40         # agent review + findings writing
python-dotenv>=1.0      # loads .env (ANTHROPIC_API_KEY) automatically
